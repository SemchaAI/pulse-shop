import css from '../../(root)/assets/home/page.module.scss';
export default function AdminPage() {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        flexDirection: 'column',
        alignItems: 'center',
        height: 'calc(100% - 76px)',
      }}
    >
      here will be some statistics
    </div>
  );
}
