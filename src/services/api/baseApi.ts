import * as products from '../productsApi';
import * as filters from '../filtersApi';
import * as variants from '../variantApi';
import * as cart from '../cartApi';
import * as favorite from '../favoriteApi';
import * as user from '../userApi';
import * as rate from '../rateApi';
import * as category from '../categoryApi';
import * as color from '../colorApi';

export const api = {
  filters,
  products,
  variants,
  cart,
  favorite,
  user,
  rate,
  category,
  color,
};
